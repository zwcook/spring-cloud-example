server:
  port: 8011
spring:
  application:
    # 服务注册到注册中心的名称
    name: provider
eureka:
  client:
    service-url:
      # 注册中心的地址配置
      defaultZone: http://localhost:8761/eureka/
  instance:
    # 是否将当前服务的IP注册到Eureka Server
    prefer-ip-address: true