# 注册中心端口
server:
  port: 8761
eureka:
  client:
    # 是否在注册中心把当前服务进行注册
    register-with-eureka: false
    #是否要同步其他注册中心的数据
    fetch-registry: false
    service-url:
      #访问路径
      defaultZone: http://localhost:8761/eureka

